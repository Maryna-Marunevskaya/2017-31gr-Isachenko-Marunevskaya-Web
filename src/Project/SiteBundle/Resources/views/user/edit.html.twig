{% extends 'ProjectSiteBundle::base.html.twig' %}
{% block title %}Редактирование пользователя - Moorooso{% endblock %}

{% block body %}
    {{ form_start(form, { 'action': path('ProjectSiteBundle_user_edit', { 'id' : user.id }), 'method': 'POST'}) }}
    {{ form_errors(form) }}
    {{ form_row(form.username) }}
    {{ form_row(form.originalPassword) }}
    {{ form_row(form.role) }}
    {{ form_rest(form) }}
    <input type="submit" value="Сохранить" class="button" />
{% endblock %}
